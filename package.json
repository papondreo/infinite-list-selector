{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd server/client && npm install && npm run build",
    "start": "node server/src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5"
  }
}
